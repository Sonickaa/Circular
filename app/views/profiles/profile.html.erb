
 <h3> Guten Tag</h3>
  <h3> <%= current_user.first_name  %></h3>
  <h3> <%= current_user.last_name %></h3>
  <p><%= current_user.bio %></p>
<h2>my own items</h2>
<%= link_to "Profile details", edit_profile_path(@user) %>
<%= link_to "Log Out", destroy_user_session_path, data: {turbo_method: :delete} %>
